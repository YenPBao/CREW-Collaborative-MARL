# ============================================================
# CREW — runtime dependencies
# Install with: pip install -r requirements.txt
# ============================================================

# ----- Core RL / numerics -----
torch>=2.1.0
numpy>=1.24.0
gymnasium>=0.29.0

# ----- LLM API clients -----
openai>=1.0.0            # OpenAI, OpenRouter and vLLM (OpenAI-compatible)
anthropic>=0.20.0        # Claude
google-genai>=0.5.0      # Gemini
python-dotenv>=1.0.0     # loads .env

# ----- Embeddings (observation features + reward) -----
sentence-transformers>=2.7.0
transformers>=4.38.0
scikit-learn>=1.3.0

# ----- Data / reporting -----
pandas>=2.0.0
pydantic>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
wandb>=0.16.0            # optional, disable with --no-wandb

# ----- Optional: local GPU serving via `python src/serve.py` -----
# vllm>=0.6.0
